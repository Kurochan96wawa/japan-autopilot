name: weekly-improve

# 毎週月曜 21:00 UTC（火 6:00 JST）に成果分析→戦略更新→週次PDCA(GA4×Pinterest)
on:
  schedule:
    - cron: "0 21 * * 1"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  improve:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Run improve + SEO
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PINTEREST_ACCESS_TOKEN: ${{ secrets.PINTEREST_ACCESS_TOKEN }}
          # ④週次PDCA: GA4 Data API のサービスアカウント鍵とプロパティID
          GA4_SERVICE_ACCOUNT_JSON: ${{ secrets.GA4_SERVICE_ACCOUNT_JSON }}
          GA4_PROPERTY_ID: ${{ secrets.GA4_PROPERTY_ID }}
          # ⑤SEO改善: GSC(Search Console) のサイトURL（ドメインプロパティ）
          GSC_SITE_URL: ${{ vars.GSC_SITE_URL || 'sc-domain:littletabi.com' }}
        run: |
          python -m src.main improve
          python -m src.seo
      - name: Commit strategy/report/SEO changes
        run: |
          git config user.name  "autopilot-bot"
          git config user.email "actions@github.com"
          git add data/state.json data/weekly_report.md docs || true
          git commit -m "chore: weekly PDCA + SEO update $(date -u +%F)" || echo "no changes"
          git push || echo "push skipped"
